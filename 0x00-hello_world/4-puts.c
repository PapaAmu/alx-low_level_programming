#include <stdio.h>

/**
 * main - Entry point
 *
 * Returns : 0 all the times
 */
int main(void)
{
	char out_put[60] = "\"Programming is like building a multilingual puzzle";

	puts(out_put);

	return (0);
}
